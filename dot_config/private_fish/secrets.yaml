secrets:
  # Nexus credentials
  NEXUS_USER: "op://Secrets/Nexus/username"
  NEXUS_PASS: "op://Secrets/Nexus/password"
  NEXUS_HOST: "op://Secrets/Nexus/host"
  NEXUS_NPM_TOKEN: "op://Secrets/Nexus/token"

  # GitHub tokens
  HOMEBREW_GITHUB_API_TOKEN: "op://Secrets/Homebrew/token"

  # Testing tokens
  MAILSLURP_API_KEY: "op://Secrets/Mailslurp/credential"

  # Terraform
  TF_TOKEN_app_terraform_io: "op://Secrets/Terraform/token"
